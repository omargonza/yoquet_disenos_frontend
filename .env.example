# ================================
# 🌍 CONFIGURACIÓN GENERAL
# ================================

# Entorno actual (Render lo asigna automáticamente a "production")
NODE_ENV=development

# ================================
# 🖥️ FRONTEND (Vite)
# ================================

# URL base de la API Django
# 👉 En local usá tu backend en 127.0.0.1
# 👉 En Render, usá la URL del servicio backend
VITE_API_URL=http://127.0.0.1:8000

# ================================
# 🔐 AUTENTICACIÓN (SimpleJWT)
# ================================

# Endpoints (solo si querés sobreescribirlos)
# Por defecto: /api/token/ y /api/token/refresh/
# VITE_AUTH_LOGIN=/api/token/
# VITE_AUTH_REFRESH=/api/token/refresh/

# ================================
# ⚙️ OTROS (opcional)
# ================================

# Modo debug para mostrar logs en consola
VITE_DEBUG=false

# Número de WhatsApp para pedidos (código de país + número, sin espacios)
VITE_WHATSAPP_NUMBER=541126483009
